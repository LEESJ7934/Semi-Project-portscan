# DB 계정정보는 프로젝트 루트의 .env에서 불러옵니다.
db:
  source: environment

scan_defaults:
  target: "127.0.0.1"
  scan_type: "tcp"
  port_range: "1-1024"
  timeout: 1.0
  max_workers: 100
  banner_grab: true

api:
  host: "127.0.0.1"
  port: 5000